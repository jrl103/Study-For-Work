import styled from 'styled-components';
import Router from './Router';

export default function Main() {
  return (
    <S.Main>
      <Router />
    </S.Main>
  );
}

const S = {
  Main: styled.div``,
};
